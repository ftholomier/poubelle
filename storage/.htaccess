# Dossier hors racine web. Ce filet ne sert que si l'hébergement impose
# un DocumentRoot à la racine du projet : rien n'est servi depuis ici.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
